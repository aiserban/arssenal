import { whileStatement } from '@babel/types';
import * as React from 'react';
import { Text } from 'react-native';

export default function ModalTitle(props: any) {
    return(
        <Text style={{ backgroundColor: 'white', alignSelf: 'center', width: 100}}>yada yada</Text>
    )
}